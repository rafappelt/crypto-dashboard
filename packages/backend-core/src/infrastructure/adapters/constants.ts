/**
 * Default maximum number of reconnection attempts for WebSocket connections.
 * After this number of attempts, the adapter will stop trying to reconnect.
 */
export const DEFAULT_MAX_RECONNECT_ATTEMPTS = 10;

